# Stablecoin Research Hub — 重构施工清单

> 用法:把本文件作为工作简报交给 Claude Code,按顺序逐个 commit 执行。
> 原则(来自规格书):**小颗粒、可评审的 commit;一个模块完整做完再做下一个;实现冲突时以规格书为准。**
> 五大模块 = Dashboard / Resources / Experts(Authors) / Regulatory / Our Research。

源码位置:
- DB schema：`lib/db/src/schema/`
- API 路由：`artifacts/api-server/src/routes/`
- 前端页面：`artifacts/stablecoin-hub/src/pages/`

---

## 阶段 0 — 数据模型(P0,三个模块的地基,必须最先做)

### Commit 0.1 — 新建 institutions 表
- 文件:新增 `lib/db/src/schema/institutions.ts`
- 字段:`id, name(unique), country, website, createdAt`
- 在 `schema/index.ts` 导出
- 验收:`pnpm --filter @workspace/db run push` 成功建表

### Commit 0.2 — 新建 authors 实体 + 资源-作者连接表
- 新增 `lib/db/src/schema/authors.ts`
- `authors`:`id, name, affiliation, researchInterests text[], institutionId → institutions.id(多对一,可空)`
- `resource_authors`:`resourceId, authorId`(复合主键;资源↔作者**多对多**)
- 验收:两张表建成,外键正确

### Commit 0.3 — resources 表补 institution 字段 + 修正 sourceType
- 文件:`lib/db/src/schema/resources.ts`
- 加 `institution text`(Method A 要求)
- `sourceTypeEnum` 去掉 `"Experts & Scholars"`(专家是独立模块,不是资源类型);改为规格的资源类型:`Journal Paper / Working Paper / Report / Gov Document / News / Think Tank Report`
- 同步改 `routes/resources.ts` 里 Gemini prompt 的枚举说明(约 270 行)
- 验收:typecheck 通过,导入流程返回的 sourceType 合法

### Commit 0.4 — 状态机补全
- `resourceStatusEnum` 改为:`uploading / processing / pending / approved / rejected / failed`
- 前端 `academic-resources.tsx:13` 的 `ResourceStatus` 类型同步
- 验收:导入失败时记录 `failed`,处理中记录 `processing`

### Commit 0.5 — 新建 regulatory 实体 + 监管-资源连接表
- 新增 `lib/db/src/schema/regulatory.ts`
- `regulatory`:`id, title, country, date, summary, link, createdAt`
- `regulation_resources`:`regulationId, resourceId`(监管↔资源**多对多**)
- 验收:建表成功

---

## 阶段 1 — Dashboard 重构(P1,规格最高优先级)

### Commit 1.1 — 删除所有管理入口
- 文件:`pages/dashboard.tsx`
- 删除 247–255 的 Quick Actions / Submit Research Paper / Add Resource via AI 整块
- 删除所有指向 `/research`(Our Research)的入口
- 验收:Dashboard 上不存在任何提交/上传/跳转 Our Research 的按钮

### Commit 1.2 — 新建聚合统计接口
- 新增 `routes/dashboard.ts`,`GET /api/dashboard/stats`
- 返回:`total_resources, recent_resources, growth_trend(近30天), distribution_by_type, distribution_by_country, regulatory_activity, top_authors, top_tags`
- 在 `routes/index.ts` 挂载
- 验收:接口返回真实聚合数据(不再前端 split 计算)

### Commit 1.3 — Dashboard 可视化
- `dashboard.tsx` 改为只读可视化:总量卡片、最新上传、增长趋势图、资源分布图、监管活动、作者统计
- 用现有 `components/ui/chart.tsx` 渲染
- **Our Research 数据不得出现在此页**
- 验收:页面无任何管理功能,只有图表与统计

---

## 阶段 2 — Resources 上传中心(P1)

### Commit 2.1 — 后端补全状态流转
- `routes/resources.ts` 导入流程持久化 `processing` / `failed`
- 保留现有 admin 直通 approved、普通用户 pending 的逻辑(已正确)

### Commit 2.2 — Method C:PDF 导入 + OCR(目前完全缺失)
- 后端加 multer 接收文件;OCR(tesseract)→ 抽 DOI → 联网检索补全元数据 → 入库
- 支持单个 + 多个 PDF
- 验收:上传 PDF 能自动出元数据并进入待审

### Commit 2.3 — 前端上传中心三方法 + 进度
- `academic-resources.tsx` 三个 tab:Manual / DOI·URL Import / PDF Import
- 上传队列、进度条、百分比、当前任务、批量进度
- Manual 表单补 `institution` 字段
- 验收:三种方法可用,进度 UI 完整

---

## 阶段 3 — Authors 模块(P1)

### Commit 3.1 — Authors 接口
- 新增 `routes/authors.ts`:`GET /api/authors`、`GET /api/authors/:id`(含该作者在本平台的全部资源)
- 资源创建/导入时,作者名 upsert 进 authors 表并写 resource_authors 关联

### Commit 3.2 — 前端改用真实作者数据
- `experts.tsx` / `author.tsx` 改查 `/api/authors`,不再从 `/api/resources` 解析字符串
- 全站作者名可点击 → 跳 `/authors/:id`,展示 profile / 机构 / 本平台全部资源
- 把 Experts 加入主导航(目前 `layout.tsx` 的 NAV_ITEMS 里没有)

---

## 阶段 4 — Our Research 鉴权(P1,安全)

### Commit 4.1 — 加权限 + 发布状态
- `routes/our_research.ts`:`POST`/`PATCH`/`DELETE` 加 `requireAuth, requireAdmin`
- 表加 `published boolean`;普通用户列表只返回已发布
- 验收:非管理员无法创建/删除;未发布项不对普通用户可见

---

## 阶段 5 — Regulatory 模块(P2)

### Commit 5.1 — Regulatory 接口
- 新增 `routes/regulatory.ts`:CRUD + country 过滤 + 时间线 + 统计聚合
- 支持与资源的多对多关联

### Commit 5.2 — 前端接真实数据
- `regulatory.tsx` 去掉硬编码,接 `/api/regulatory`
- country 过滤、时间线、统计

---

## 验收总览(对照规格书五条核心约束)

- [ ] Dashboard 纯可视化,无任何管理功能,无 Our Research 入口
- [ ] Resources 支持三种上传方式 + 完整状态机 + 进度
- [ ] Authors 是独立实体,与资源多对多,全站可点击
- [ ] Regulatory 是独立记录,可与资源关联,有国家/时间线/统计
- [ ] Our Research 仅管理员可写,普通用户只读已发布
